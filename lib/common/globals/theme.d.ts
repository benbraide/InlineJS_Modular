import { GlobalHandler } from './generic';
export declare class ThemeGlobalHandler extends GlobalHandler {
    private persistent_;
    private scopeId_;
    private proxy_;
    private props_;
    constructor(persistent_?: boolean);
}
